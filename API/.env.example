# MongoDB Connection (Optional - defaults to localhost if not set)
MONGODB_URI=mongodb://localhost:27017/

# For MongoDB Atlas (cloud), use:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/
